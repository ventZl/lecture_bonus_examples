#pragma once

template<typename T>
struct remove_pointer;
// TODO: Implement this template so if will provide a type definition that will always return input type T stripped of pointer (if present)
// It will retain all other qualifiers.
